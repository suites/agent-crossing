{
  "name": "@agent-crossing/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn api.main:app --reload --app-dir src",
    "start": "uv run uvicorn api.main:app --app-dir src"
  }
}
