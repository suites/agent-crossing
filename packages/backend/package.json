{
  "name": "@agent-crossing/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "export PYTHONPATH=$PYTHONPATH:$(pwd)/src && uv run uvicorn api.main:app --reload",
    "start": "export PYTHONPATH=$PYTHONPATH:$(pwd)/src && uv run uvicorn api.main:app"
  }
}
